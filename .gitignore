# --- FOLDER UTAMA (PENTING) ---
# Folder ini sangat berat, jangan di-upload. Cloudflare akan instal sendiri nanti.
node_modules/
# Folder hasil build (kompilasi), jangan di-upload.
dist/
dist-ssr/
*.local

# --- KEAMANAN & RAHASIA (SANGAT PENTING) ---
# Baris ini akan menyembunyikan API KEY Gemini kamu agar tidak dicuri orang.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Menyembunyikan semua file yang berakhiran .local
*.local

# --- LOGS & ERROR ---
# File catatan error yang tidak perlu ada di GitHub.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- EDITOR & SISTEM OPERASI ---
# File sampah otomatis dari aplikasi koding atau sistem komputer.
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- CADANGAN (BACKUP) ---
# Jika kamu punya folder backup manual di dalam proyek.
*.bak
*.tmp
